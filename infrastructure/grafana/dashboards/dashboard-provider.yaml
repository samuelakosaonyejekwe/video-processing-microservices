apiVersion: 1

providers:

  - name: ${GRAFANA_DASHBOARD_PROVIDER_NAME}

    orgId: ${GRAFANA_ORG_ID}

    folder: ${GRAFANA_DASHBOARD_FOLDER}

    type: file

    disableDeletion: ${GRAFANA_DISABLE_DASHBOARD_DELETION}

    editable: ${GRAFANA_DASHBOARD_EDITABLE}

    options:

      path: ${GRAFANA_DASHBOARD_PATH}