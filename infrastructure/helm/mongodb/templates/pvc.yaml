apiVersion: v1
kind: PersistentVolumeClaim

metadata:

  name: mongodb-pvc

spec:

  accessModes:

    - ReadWriteOnce

  storageClassName: {{ .Values.persistence.storageClass | default .Values.global.storage.className | default "" }}

  resources:

    requests:

      storage: {{ .Values.persistence.size }}