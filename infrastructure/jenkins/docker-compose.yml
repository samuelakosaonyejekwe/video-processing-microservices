version: "3.9"

services:

  jenkins:

    build:
      context: .
      dockerfile: Dockerfile

    container_name: ${JENKINS_CONTAINER_NAME:-jenkins}

    restart: always

    user: jenkins
    group_add:
      - docker

    ports:
      - "${JENKINS_HTTP_PORT:-8080}:8080"
      - "${JENKINS_AGENT_PORT:-50000}:50000"

    environment:

      CASC_JENKINS_CONFIG: /var/jenkins_home/casc_configs

      JENKINS_ADMIN_ID: ${JENKINS_ADMIN_USERNAME}

      JENKINS_ADMIN_PASSWORD: ${JENKINS_ADMIN_PASSWORD}

      JENKINS_URL: ${JENKINS_URL}

      GITHUB_REPOSITORY: ${GITHUB_REPOSITORY}

      GITHUB_BRANCH: ${GITHUB_BRANCH}

      JAVA_OPTS: >-
        -Djenkins.install.runSetupWizard=false
        -Djava.awt.headless=true

    env_file:
      - .env

    volumes:

      - jenkins_home:/var/jenkins_home

      - ./casc:/var/jenkins_home/casc_configs

      - ./init.groovy.d:/var/jenkins_home/init.groovy.d

      # SECURITY WARNING: mounting the host Docker socket grants this container
      # full root-equivalent control over the Docker host. Any pipeline able to
      # run `docker` here can escape to the host. This is a known risk accepted
      # for local/dev Jenkins builds only.
      # RECOMMENDED hardening for shared/production hosts: replace this bind
      # mount with a read-scoped socket proxy (e.g. tecnativa/docker-socket-proxy)
      # exposing only the required Docker API endpoints over the network, and
      # point DOCKER_HOST at the proxy instead of mounting the raw socket.
      - /var/run/docker.sock:/var/run/docker.sock

    networks:
      - jenkins-network

    healthcheck:

      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://localhost:8080/login"
        ]

      interval: 30s

      timeout: 10s

      retries: 5

      start_period: 120s

volumes:

  jenkins_home:

    driver: local

networks:

  jenkins-network:

    driver: bridge