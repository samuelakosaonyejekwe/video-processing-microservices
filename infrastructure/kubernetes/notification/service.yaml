apiVersion: v1

kind: Service

metadata:
  name: notification-service
  namespace: video-converter

spec:

  selector:
    app: notification

  ports:

    - protocol: TCP
      port: 80
      targetPort: 8003

  type: ClusterIP