apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: shared-storage-pvc
  namespace: video-converter

spec:

  accessModes:
    - ReadWriteOnce

  storageClassName: video-storage-class

  resources:

    requests:
      storage: 20Gi