apiVersion: networking.k8s.io/v1

kind: NetworkPolicy

metadata:
  name: postgres-network-policy
  namespace: ${DATABASE_NAMESPACE}

spec:
  podSelector:
    matchLabels:
      app: ${POSTGRES_APP_NAME}

  policyTypes:
    - Ingress
    - Egress

  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ${K8S_NAMESPACE}
          podSelector:
            matchExpressions:
              - key: app
                operator: In
                values:
                  - auth-service
                  - postgres-migrations

      ports:
        - protocol: TCP
          port: ${POSTGRES_PORT}

  egress:

    # DNS — required for cluster service resolution.
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ${KUBE_SYSTEM_NAMESPACE}
      ports:
        - protocol: UDP
          port: ${DNS_PORT}
        - protocol: TCP
          port: ${DNS_PORT}
