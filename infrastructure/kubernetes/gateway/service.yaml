apiVersion: v1

kind: Service

metadata:
  name: gateway-service
  namespace: ${K8S_NAMESPACE}

spec:

  selector:
    app: gateway

  ports:

    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080

  type: ClusterIP
