kind: Secret

metadata:
  name: mongodb-secret
  namespace: video-converter

type: Opaque

stringData:
  MONGO_USERNAME: "${MONGO_USERNAME}"
  MONGO_PASSWORD: "${MONGO_PASSWORD}"
  MONGO_URI: "${MONGO_URI}"

