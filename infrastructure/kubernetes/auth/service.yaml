apiVersion: v1

kind: Service

metadata:
  name: auth-service
  namespace: video-converter

spec:

  selector:
    app: auth

  ports:

    - protocol: TCP
      port: 80
      targetPort: 8001

  type: ClusterIP