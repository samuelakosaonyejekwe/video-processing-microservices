apiVersion: v1

kind: Service

metadata:
  name: auth-service
  namespace: ${K8S_NAMESPACE}

spec:

  selector:
    app: auth

  ports:

    - protocol: TCP
      port: 80
      targetPort: 8001

  type: ClusterIP